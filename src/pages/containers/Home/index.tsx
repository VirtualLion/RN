import React from 'react';
import {
    Text,
} from 'react-native';

interface Props {}
interface State {}

export default class Home extends React.Component <Props, State> {
    render() {
        return (
            <Text>123</Text>
        );
    }
}